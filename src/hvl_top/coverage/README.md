# MAC Coverage

`mac_cov.sv` contains the functional coverage collector. Keep coverage points
feature-oriented as tests grow: TX, RX, control/PAUSE, registers, statistics,
and feature interactions. Use coverage-enabled nightly regressions and merge
their UCDB files before making closure decisions.
