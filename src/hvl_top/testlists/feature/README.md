# Feature Testlists

Each file is the executable subset of one feature. Add a stable testcase to
its feature list first, then promote it to `daily.list`, `nightly.list`, or
`full_regression.list` as appropriate. Planned tests remain only in the
testplan until their UVM classes exist.
